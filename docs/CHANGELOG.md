# История изменений

## 2026-09-04

- Добавлен поиск по фотографиям и альбомам галереи: переиспользуемая форма поиска над списками, результаты через query-параметр ?search=, пустой результат - сообщение с запросом
- Добавлен поиск по статьям блога: переиспользуемая форма поиска над списком, результаты через query-параметр ?search=
- Скилл github-workflow дополнен правилами: merge PR выполнять только после зеленого CI (при незавершенных checks - дождаться, при падении - не мержить), в задачах и PR сразу указывать пользователя исполнителем (--assignee @me)
- Markdown-документация приведена к единому формату: абзац и пункт списка - одна строка без жестких переносов; развертывание выполняют скрипт tools/unwrap_markdown.py и pre-commit хук unwrap-markdown, в AGENTS.md добавлено правило формата с явным отказом от MD013
- Деплой на VPS в GitHub Actions переведен на ручной запуск (workflow_dispatch): автоматическая доставка после Release image отключена - промежуточные мержи отдельных PR не попадают в прод до выполнения общей цели, параллельные запуски выстраиваются в очередь
- Добавлен скилл github-workflow: типовой цикл работы с GitHub через gh - задачи, PR, проверка CI, merge, мониторинг release и деплоя

## 2026-09-02

- Автоматическая синхронизация зеркал (main и теги) - обертки CI: trunk-workflow в SourceCraft (пуши в основную ветку и теги) и trunk.yml в GitHub Actions (пуши, ручной запуск); ключ доставляется в ssh-agent через stdin, файлы с ключами не создаются, сбой синхронизации не прерывает конвейер
- Синхронизация main и тегов между GitHub и SourceCraft: скрипт tools/sync_mirrors.py (заменяет tools/sync-branch.sh) - состояния зеркал получаются во временные служебные ссылки текущего репозитория с очисткой при завершении, fast-forward push только отстающим, при расхождении веток или конфликте тегов - код 1 и диагностика без изменения рефов

## 2026-09-01

- Отчеты о резервном копировании (тема и шапка письма) и имена файлов дампов переведены с UTC на МСК: время в логах, письмах и именах дампов единообразно
- Установщики setup.sh обоих режимов развертывания фиксируют системную таймзону Europe/Moscow: расписание cron-задач (бэкап 23:01, продление сертификата 04:17) исполняется по МСК, а не по UTC хоста
- Документация развертывания (backups.md, proxy.md) и скилл backup-audit указывают время расписания с явной таймзоной
- Формулировки AGENTS.md сжаты без изменения правил: дублирующиеся нормы (ruff, mypy, pytest, pre-commit, русский язык, профили Compose, ImageKit) сведены к одному месту, разделы перегруппированы по темам

## 2026-08-31

- Диспетчер systemd-деплоя выполняет systemctl enable юнита personal-website.service: переключение механизма развертывания не оставляет сервис незарегистрированным в автозапуске
- production-diagnostics берет путь рабочей копии прода из секрета PROJECT_DIR вместо устаревшего жесткого каталога
- Методы S3-хранилища (exists, delete, get_modified_time, read_bytes, listdir, rmtree, replace, copy_file) адресуют ключи бакета с учетом префикса location: устранена перегенерация кэша ImageKit и ошибка 500 API галереи в S3-режиме с раскладкой media/
- Проверка --verify дампов БД не считает ошибкой завершение источника конвейера по SIGPIPE: исправлена ложная ошибка «дамп не читается pg_restore -l»

## 2026-08-30

- Команды резервного копирования (backup, backup_db, backup_media) отправляют HTML-отчет о результате на BACKUP_NOTIFY_EMAIL: успех - цели, объемы и ретеншн, ошибка - полный журнал команды; проверка целей (--verify) не уведомляет, отказ отправки не влияет на код возврата
- Путь деплоя в CI/CD-workflow вынесен в секрет PROJECT_DIR: жестко прописанные каталоги (/srv/personal-website в SourceCraft, /root/personal-website в GitHub) заменены переменной; значение задается секретами обеих платформ и должно совпадать с путем рабочей копии репозитория на сервере

## 2026-08-29

- Исправлена потеря vendor-статики (bootstrap/dist, tinymce) в Docker-образе: сборка фронтенда копирует ее в frontend/dist, устаревшие копии из backend/staticfiles удаляются при сборке
- entrypoint.sh настраивает алиас mc при старте контейнера из переменных MINIO_ALIAS, MINIO_SERVER_URL, MINIO_ACCESS_KEY и MINIO_SECRET_KEY: команды резервного копирования выполняются внутри контейнера и без алиаса теряли доступ к mc-целям после каждого пересоздания
- Медиа в S3-бакете хранятся в префиксе media/, статика - в static/: резервное копирование медиа не затрагивает статику; для существующих бакетов - разовый перенос объектов по рецепту storage.md
- Смена типа хранилища выполняется через постоянные цели бэкапов: backup_media на старом хранилище, restore_media на новом; временные цели и правки .env на время переноса не применяются
- create_data_directories в setup.sh передает каталоги данных сервис-пользователю рекурсивно: файлы, оставшиеся от контейнерного запуска, блокировали старт systemd-сервиса; порядок перехода описан в application.md

## 2026-08-28

- Созданы документы docs/deployment/: matrix.md (параметры и типовые сценарии развертывания), application.md, database.md, storage.md, proxy.md, backups.md; .env.example перестроен секциями по параметрам
- Документация docs/deployment/ переписана связной прозой; правило закреплено в AGENTS.md
- Traefik заменен на nginx + certbot: официальный образ nginx (профиль nginx) с шаблоном конфигурации, раздача медиа filesystem-хранилища прокси с диска, certbot отдельным контейнером (выпуск standalone в scripts/docker/setup.sh, продление webroot по cron через scripts/docker/renew-cert.sh), self-signed сертификат локального стека - задача task dev-cert; переменные TRAEFIK_* заменены на HTTP_PORT/HTTPS_PORT
- Добавлен интеграционный тест раздачи медиа через прокси (tests/test_proxy.py)
- scripts/server/setup.sh показывает состояние запущенных сервисов
- Добавлены скиллы backup-restore, backup-audit, restore-drill; скиллы postgresql-dump/postgresql-restore переведены на соглашения системы бэкапов
- Проверка --verify медиа-целей сообщает о любом расхождении числа объектов с источником
- Ops-разделы README.md вынесены в docs/deployment/
- markdownlint охватывает все markdown-файлы репозитория

## 2026-08-26

- Добавлен вариант systemd-режима с unix-сокетом (socket activation): юниты в scripts/server/systemd/socket/, подраздел в application.md; дефолт остается TCP на 127.0.0.1
- Из шаблонов конфигурационных файлов (systemd-юнит, vhost'ы nginx) удалены комментарии: логика описана в docs/deployment/
- Термин «ось» изъят из документации: параметры развертывания называются естественными словами, «эталонные конфигурации» переименованы в «эталонные сценарии развертывания»
- Созданы рецепты развертывания docs/deployment/proxy.md и application.md (хостовый nginx + certbot, Traefik в compose, docker run, systemd Gunicorn)
- Добавлена переменная TRAEFIK_ACME_CASERVER (ACME staging-каталог для тестовых сред Traefik)
- Скрипты scripts/server/ переработаны под текущую архитектуру: юнит systemd, vhost nginx из шаблона, выпуск сертификата certbot webroot; удалены устаревшие makelink.sh и шаблоны backend/config/
- Скилл server-setup дополнен матрицей профилей compose и двумя режимами прокси
- Исправлена compose-ветка scripts/backup.sh: команды приложения запускаются интерпретатором виртуального окружения

## 2026-08-25

- Резервное копирование медиа при STORAGE_TYPE=s3 выполняется в цели напрямую, без промежуточной копии в TEMP_ROOT
- Восстановление и проверка дампов БД выполняются конвейером mc cat / rclone cat | pg_restore без выгрузки на диск
- Механизмы копирования выделены в плагины-цели (McTarget, RcloneTarget) с реестром TARGET_PREFIXES
- Приложение backup декомпозировано на модули process.py, targets.py, utils.py, services.py
- MC_PATH требуется только при наличии mc-целей
- Докстринги модулей приложения backup дополнены секциями Args/Returns/Raises

## 2026-08-22

- Резервное копирование реализовано Django-приложением backend/backup/: команды backup, backup_db, backup_media (с --verify), restore_db, restore_media; вход - scripts/backup.sh по расписанию cron
- БД: дамп pg_dump -Fc с проверкой pg_restore -l, копирование в BACKUP_DB_TARGETS, ретеншн BACKUP_DB_RETENTION_DAYS
- Медиа: зеркальная синхронизация в BACKUP_MEDIA_TARGETS, восстановление из цели, verify
- Цели бэкапов описываются в .env записями BACKUP_TARGET_<ИМЯ> с префиксами mc: и rclone:; rclone добавлен в Docker-образ
- Диспетчер деплоя выполняет предварительное резервное копирование вызовом scripts/backup.sh backup
- Добавлены тесты приложения backup

## 2026-08-20

- В AGENTS.md зафиксировано: скрипты деплоя не запускаются на локальной машине разработчика
- Добавлен shellcheck в pre-commit; исключения для source рантайм-файлов объявлены в .shellcheckrc
- Добавлен диспетчер деплоя scripts/deploy.sh: единый вход для CI/CD и ручного обновления, механизм выбирается переменной DEPLOY_MODE
- Отсутствие .env - фатальная ошибка в s3backup.sh и s3restore.sh
- Скрипт scripts/docker/update.sh удален как дублирующий deploy.sh; очистка образов ограничена образами проекта
- Переменная DEPLOY_MODE описана в .env.example
- s3backup.sh и s3restore.sh поддерживают перенос медиа между бэкендами при смене STORAGE_TYPE
- Загрузка .env в s3backup.sh и s3restore.sh переведена на безопасную (set -a + source)
- Ограничение --limit-download убрано из зашитых флагов s3restore.sh
- Добавлен блок безопасности за обратным прокси (SECURE_*, cookie flags, SECURE_PROXY_SSL_HEADER)
- CSRF_TRUSTED_ORIGINS собирается из DOMAIN_NAME
- Записи домена в ALLOWED_HOSTS и CORS_ALLOWED_ORIGINS добавляются только при заданном DOMAIN_NAME
- В .env.example все переменные показаны раскомментированными со значениями по умолчанию
- Добавлен pre-commit хук fix-typography (длинное тире -> дефис, стрелка -> "->")

## 2026-08-19

- Сервис приложения в compose.yaml переименован из website в application
- Инфраструктурные сервисы переведены с профиля dev на собственные профили postgres, minio и traefik
- Порт приложения публикуется на 127.0.0.1:${DJANGO_PORT}; порты postgres и MinIO перевязаны на 127.0.0.1
- Сервису application добавлена запись extra_hosts host.docker.internal:host-gateway
- Healthcheck контейнера переключен на эндпоинт /health/ (SELECT 1)
- Ожидание готовности хранилища в entrypoint.sh выполняется только при STORAGE_TYPE=s3 и проверяет эндпоинт по TCP
- Настройка алиаса MinIO в entrypoint.sh удалена
- Число воркеров Gunicorn задается переменной GUNICORN_WORKERS
- Задача Taskfile test-integration дожидается healthcheck перед запуском тестов
- Документация и .env.example синхронизированы с новыми именами сервисов и профилями

## 2026-08-17

- Имя образа приложения в compose.yaml унифицировано с публикуемым SourceCraft CI (mikhpo/personal_website, подчёркивание): прежде compose ссылался на несуществующий образ с дефисом, и docker compose pull падал ошибкой object not found
- entrypoint.sh: настройка алиаса MinIO пропускается при STORAGE_TYPE=s3 - команда mc admin info неприменима к внешнему S3-хранилищу и уводила контейнер в crash-loop
- entrypoint.sh: заменён eval export файла .env на безопасную загрузку с проверкой существования - при отсутствии файла export без аргументов печатал значения секретных переменных в логи контейнера
- Инструкции развертывания (README, CONTRIBUTING) дополнены адресом клонирования SourceCraft как основным; задокументировано экранирование символа доллара в значениях .env для Docker Compose ($$, раздел CONTRIBUTING «Экранирование символа доллара»): без удвоения compose молча искажает значения вроде SECRET_KEY
- deploy-workflow SourceCraft переключён на целевой каталог /srv/personal-website

## 2026-08-16

- Gunicorn в контейнере запускается с рециклингом воркеров (--max-requests=500, --max-requests-jitter=50): после обработки фотографий (Pillow, ImageKit) воркер удерживает память и не возвращает её аллокатору, перезапуск воркера по счётчику запросов ограничивает дрейф RSS (замер: рост воркера со ~110 до ~260 МБ после пиковой загрузки фото)

## 2026-08-15

- Прокси-сервер nginx заменён на Traefik (v3.7) с автоматическими сертификатами Let's Encrypt; каталог nginx/ и том nginx-certs удалены
- Состояние ACME-клиента Traefik хранится на хосте (bind-mount traefik/letsencrypt/acme.json)
- Выпуск сертификатов управляется переменной TRAEFIK_CERT_RESOLVER
- Публикация порта приложения 8000 наружу прекращена: весь трафик проходит через Traefik
- Логирование контейнеров переведено на json-file с ротацией (якорь x-logging)
- Логгер django.request подключён к консольному хэндлеру
- scripts/docker/deploy.sh перепрофилирован под неинтерактивный деплой из CI/CD; первичная настройка вынесена в scripts/docker/setup.sh
- Из скрипта обновления bare-metal-развертывания убран вызов pgcert.sh
- Скрипты bare-metal-развертывания переименованы: deploy.sh -> setup.sh, update.sh -> deploy.sh
- deploy-workflow SourceCraft и GitHub workflow переключены на scripts/docker/deploy.sh
- Из Dockerfile убраны пакет cron и вызов scripts/cronjobs.sh
- Порт приложения вынесен в переменную DJANGO_PORT; NGINX_PORT переименована в TRAEFIK_HTTP_PORT
- Интеграционные тесты прокси адаптированы под Traefik (tests/test_traefik.py)
- Документация дополнена разделами о Docker-развертывании и сертификатах; скилл production-diagnostics переписан под Docker-окружение
- C4-диаграмма контейнеров дополнена компонентами Traefik, Docker Engine и реестрами образов

## 2026-08-14

- Сервисы postgres и minio переведены на профиль dev, активируется переменной COMPOSE_PROFILES
- Зависимости depends_on заменены ожиданием готовности сервисов в entrypoint.sh (функция wait_for_port)
- В .env.example добавлена переменная COMPOSE_PROFILES
- Задачи Taskfile up-detached, watch и restart обновлены под профиль dev
- Документация дополнена описанием профилей Docker Compose
- Раздача статики в S3-режиме переведена на объектное хранилище, локально продолжает работать WhiteNoise
- Сборкам статики с хешем в имени назначается бессрочное immutable-кэширование
- STATIC_URL и webpack publicPath вынесены в переменные окружения
- Запросы /favicon.ico и /robots.txt обслуживаются постоянными редиректами Django на статическое хранилище
- WhiteNoise отключается в S3-режиме
- Из конфигураций nginx удалены файловые локации статики и медиа
- Интеграционные тесты nginx обновлены под редиректы favicon и robots.txt
- Каталог node_modules исключён из STATICFILES_DIRS
- Из продакшен-выгрузки статики исключены source-map'ы и документация
- Таймаут удалённой команды деплоя увеличен до 30 минут

## 2026-08-13

- Самописные скрипты бэкапа и восстановления БД заменены утилитами pg_dump и pg_restore; процедуры перенесены в документацию и скиллы
- Из scripts/server/restart.sh убран перезапуск базы данных
- Из scripts/cronjobs.sh и scripts/docker/update.sh удалены вызовы убранных скриптов

## 2026-08-11

- SSL-сертификат Managed PostgreSQL доставляется в контейнер при запуске, а не при сборке образа
- Добавлен скрипт scripts/pgcert.sh для загрузки SSL-сертификата PostgreSQL
- Из Dockerfile и compose.yaml удалены build-args; добавлен bind-mount сертификата (POSTGRES_SSL_ROOT_CERT_PATH, POSTGRES_SSL_CERT_HOST_PATH)
- Раздел по настройке базы данных перенесён из CONTRIBUTING.md в README.md
- Унифицирована логика публичности объектов с полем public: списки и sitemap показывают только public=True, детальный просмотр доступен всем
- Исправлена ошибка 404 при доступе к скрытым фотографиям по прямой ссылке
- Удалены staff-ветки в get_queryset просмотровых views и viewsets
- Запись объектов с полем public через REST API ограничена администраторами
- Вложенные связи в retrieve фильтруются по public=True
- Prev/next навигация на детальной странице фотографии строится только по публичным соседям
- На детальные страницы скрытых объектов добавлен meta robots noindex
- Расширены тесты API и views под инварианту публичности

## 2026-08-09

- Подготовлена миграция базы данных на Managed Service for PostgreSQL
- Добавлена документация по развертыванию БД в CONTRIBUTING.md
- Версия PostgreSQL клиента: 15 -> 17

## 2026-07-30

- Добавлено поле exif (JSONField) в модель Photo для хранения EXIF данных в БД вместо чтения из файла при каждом запросе
- Добавлена миграция данных для заполнения поля exif существующих фотографий
- Добавлена функция exif_value_to_json в gallery/utils.py для преобразования значений EXIF в JSON-совместимые типы
- Добавлены тесты для проверки сохранения и чтения EXIF данных из БД

## 2026-07-26

- Исправлена ошибка 500 при browsable-доступе к API: приложение django_filters добавлено в INSTALLED_APPS
- Исправлены пустые списки на страницах тега и категорий блога: генерация URL фильтрации перенесена во фронтенд

## 2026-07-24

- Реорганизована структура проекта в монорепозиторную архитектуру с разделением на backend и frontend
- Реализован REST API с JWT аутентификацией, кастомной пагинацией и правами доступа
- Добавлены сериализаторы и viewsets для приложений blog и gallery с полным CRUD функционалом
- Создано централизованное приложение api для управления REST API
- Мигрирован frontend на React с компонентной архитектурой
- Реализованы React компоненты для всех основных разделов: блог, галерея, навигация, пагинация
- Добавлены кастомные React хуки для работы с API данными
- Интегрирован Webpack с Babel для сборки React приложений
- Настроен процесс тестирования React компонентов с Jest
- Добавлены компоненты для загрузки фотографий с drag-and-drop и прогрессом загрузки
- Обновлён редактор WYSIWYG на TinyMCE
- Настроена интеграция React компонентов через Django template теги с автоматическим монтированием
- Добавлены команды управления для генерации тестовых объектов блога и галереи
- Обновлены CI/CD пайплайны для поддержки новой структуры проекта
- Добавлены навыки (skills) для статического анализа кода и обновления CHANGELOG

## 2025-12-25

- Добавлена проверка переменной окружения STORAGE_TYPE в скрипт резервного копирования S3 для предотвращения ненужных операций резервного копирования при использовании S3 в качестве хранилища

## 2025-12-24

- Реализована поддержка S3-совместимого хранилища (Amazon S3 или MinIO) в дополнение к локальной файловой системе
- Добавлена возможность переключения между локальной файловой системой и S3-хранилищем через настройку STORAGE_TYPE
- Созданы кастомные классы хранилищ CustomFileSystemStorage и CustomS3Storage с расширенным функционалом
- Обновлены тесты галереи и утилиты для работы через абстрактный storage API вместо прямых обращений к файловой системе

## 2025-11-28

- Добавлена конфигурация CI/CD для SourceCraft

## 2025-11-15

- Добавлена стадия деплоя на VPS
- Оптимизирована сборка образа в Dockerfile

## 2025-08-21

- Исправлена проблема определения времени выдержки в приложении галереи, приводившая к ошибке 500.
- Исправлена проблема при генерации значений объектов фабриками в тестах, когда нарушалось ограничение уникальности полей.

## 2025-08-10

- Добавлены джобы пайлайна для тестирования контейнеризованного приложения.
- Вместо сборки образа при релизе выполняется перетегировани протестированного образа.

## 2025-08-08

- Улучшена документация на проект.
- Добавлены архитектурные диаграммы.

## 2025-02-01

- Доработан механизм автоматического конфигурирования Gunicorn.

## 2025-01-06

- Внедрена система проверки типов для Python.

## 2024-12-23

- Генерация изображений и данных EXIF для тестов.

## 2024-10-26

- Добавлен скрипт для генерации диаграмм моделей.

## 2024-10-19

- Добавлен тэг шаблона для проверки наличия файлов.
- Увеличина ширина виджета TinyMCE.
- Обновлены версии зависимостей.

## 2024-10-13

- Доработка вспомогательных скриптов и команд.

## 2024-04-25

- Исправление ошибки выполнения команд MinIO Client через cron.

## 2024-04-20

- Дополнения документации в части развертывания.
- Исправления скриптов для развертывания.

## 2024-02-04

- Добавлен статический анализ кода при помощи Ruff.
- Форматирование кода для соответствия правилам Ruff.
- Добавлен статический анализ кода при помощи ESLint.

## 2024-01-28

- Добавлены скрипты для синхронизации бэкапов с S3.

## 2024-01-20

- Добавлены шаблоны задач и запросов на извлечения для GitHub.
- Рефакторинг скрипта для подготовки коммита.
- Добавлен Taskfile.yml для Task.

## 2024-01-15

- Рефакторинг скриптов для работы с бэкапами.
- Добавлены скрипты для развертывания через Docker.
- Добавлены хуки в Git.

## 2024-01-09

- Добавлены CI/CD для тестирования кода и публикации образов.

## 2024-01-06

- Nginx и Certbot добавлены в Docker Compose.

## 2024-01-01

- Приложение запускается в контейнере через Gunicorn.

## 2023-12-25

- Подготовлены инструкции по сборке и запуску контейнеров.
- Переименование каталога с модулем настроек Django.

## 2023-11-12

- Изменена сортировка фотографий в представлении списка фотографий и добавлена паджинация.
- Добавлено создание отчета Coverage по тестированию.

## 2023-11-06

- Альбомы без обложек не показываются в списке альбомов.
- Оптимизировано представление тэгов галереи.

## 2023-11-05

- Изменена структура каталогов для хранения изображений галереи.

## 2023-11-02

- Добавлены сообщения о результатах загрузки фотографий в альбом.

## 2023-10-22

- Отображение данных из EXIF в административной панели.
- Оптимизация представления карточек Bootstrap.
- Обновление версий зависимостей.

## 2023-10-21

- Добавлены кнопки для переключения между фотографиями внутри альбома.

## 2023-10-19

- Данные EXIF фотографии показываются в модальном окне.

## 2023-10-08

- Добавлен скрипт для форматирования кода и отправки коммита в удаленный репозиторий.
- Добавлена форма для пакетной загрузки фотографий в альбом.
- Добавлен подсчет количества фотографий в альбоме.

## 2023-09-28

- Настроен Pytest для прогона тестов.

## 2023-09-26

- Исправлен скрипт для обновления проекта.

## 2023-09-23

- Добавлены миниатюры и предпросмотр фотографий и обложек альбомов в административную панель.
- Добавлен показ списка фотографий при просмотре альбома в административной панели.
- Исправлен показ только публичных фотографий и альбомов в пользовательских представлениях.

## 2023-09-18

- Фотографии в административной панели сортируются от старых к новым.
- Добавлены конфигурации nginx и gunicorn.

## 2023-09-03

- Создано приложение галереи.

## 2023-01-07

- Добавлен скрипт для бэкапирования хранилища.

## 2022-12-24

- Добавлены тесты системы авторизации.

## 2022-12-22

- Добавлены тесты блога и главной страницы.
- Исправление стилей таблиц.

## 2022-11-25

- Рефакторинг структуры проекта.
- Удалено приложение для управления скриптами.
- Управление секретами через переменные окружения и .env файл.

## 2022-04-26

- Добавлены вспомогательные скрипты для управления проектом.

## 2022-01-26

- Оптимизация приложения для управления скриптами.

## 2022-01-17

- Исправления шаблонов.

## 2022-01-10

- Рефакторинг структуры проекта.

## 2022-01-08

- Доработка планировщика скриптов.

## 2021-080-07

- Оптимизация моделей и маршрутов блога.

## 2021-08-05

- Добавлена карта сайта.

## 2021-08-04

- Доработка приложения для управления скриптами.

## 2021-07-18

- Рефакторинг управления зависимостями через Poetry.
- Уравление секретами через JSON файл.

## 2021-07-05

- Добавлено приложение для управления скриптами.

## 2021-07-03

- Обновление зависимостей проекта
- Доработка механизма регистрации пользователей.

## 2021-02-22

- Первоначальный релиз.

## 2020-12-19

- Старт разработки.
